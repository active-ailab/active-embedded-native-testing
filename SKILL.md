---
name: active-embedded-native-testing
description: 为嵌入式 C/C++ 及混合原生业务代码建立 Linux/WSL 主机测试和 Zepp 设备侧 UTEST，执行 GoogleTest、ASan/UBSan、gcov、真机功能与压力验证；适用于真实源码 API、静态函数、宏分支、容量边界、故障路径和目标环境行为，不把测试框架自身覆盖率当作业务覆盖率。
---

# Embedded Native Testing

为目标业务源码建立可重复执行的主机测试和真机 UTEST，并分别报告真实业务 API、抽取辅助函数、静态审计和目标机验证范围。

该方法不是项目特有做法。Google Pigweed、Arm Mbed OS 和 Zephyr 均公开提供主机侧嵌入式测试能力。团队评审或推广时，读取 [references/industry-practice.md](references/industry-practice.md)，其中列出了公开依据、与本 skill 的对应关系及适用边界。

## 开始前

1. 读取目标仓库的 `AGENTS.md`、构建配置和项目说明。
2. 检查依赖环境，具体命令和兼容要求见 [references/environment-and-reporting.md](references/environment-and-reporting.md)。
3. 用 `git status` 识别用户已有改动；不得覆盖、回退或混入无关修改。
4. 列出本次要覆盖的业务 API、关键分支、失败路径及只能真机验证的行为。
5. 若项目提供 `components/utest`，需要真机功能或压力测试时读取 [references/zepp-device-utest.md](references/zepp-device-utest.md)。

## 选择测试方式

按证据强度优先选择：

1. **编译原始翻译单元**：通过 mock/stub 补齐外部依赖，直接调用真实业务 API。优先使用此方式。
2. **抽取真实函数体**：静态函数或完整翻译单元依赖过重时，从原源码抽取未经改写的函数体并构建 harness。必须同时校验函数签名、调用关系和相关宏，防止复制版本漂移。
3. **静态接线审计**：无法主机运行的接口，仅验证定义、调用点、函数表注册及宏守卫。不得计入运行覆盖率。
4. **目标机 UTEST**：使用项目已有设备测试框架调用真实业务实现，验证 RTOS、内存、文件系统、协议和时序行为。
5. **端到端真机验证**：涉及完整运动、落盘、同步和 APP 展示时，由设备操作和日志补充验证。

不要为了提高覆盖率复制或重写业务算法。若不得不建立等价模型，必须单独标为“模型测试”，不能声称覆盖原始源码。

长期维护时优先采用“原始 `.c` 独立编译并链接 mock”的方式。测试文件直接包含原始 `.c` 可用于访问静态函数的白盒测试，但不作为团队默认架构；抽取函数体只用于依赖尚未解耦时的过渡验证。

## 建立用例

至少覆盖与需求相关的以下类别：

- 正常流程和完整生命周期。
- 空输入、非法输入、边界值和最大时长。
- 暂停、恢复、重复进入、提前结束等状态转换。
- 内存申请失败、写入失败、容量不足和单条记录写满。
- 宏开启与关闭后的关键行为；宏值以实际 `.config` 或等价构建产物为准。
- 历史缺陷的最小复现用例。

测试应使用独立临时目录。默认清理生成的二进制、对象、`.gcda`、`.gcno` 和 `.gcov`；提供类似 `KEEP_TEST_ARTIFACTS=1` 的保留方式便于复核。

## 真机 UTEST

- 优先使用目标项目已经编译接入的测试框架；Zepp 项目先检查 `components/utest`、shell 命令、链接 map 中的 `.utest/.suite` 注册段以及当前 `NDEBUG` 配置。
- 测试文件放在业务模块自己的 `test/` 目录，通过专用测试宏参与编译；测试宏关闭或 release 构建时，不应暴露测试 API、注入逻辑或额外依赖。
- 使用 `UTEST_EXPORT` 或 `UTEST2_EXPORT` 注册用例，用 suite/path/tag 区分功能、边界和压力测试。
- 真机测试应调用真实业务实现。内部静态函数无法直接测试时，可在测试宏下增加最小薄封装，不能复制算法到测试文件。
- 功能用例覆盖真实状态序列和结果断言；压力用例通过 `key=value` 参数控制时长、循环和样本规模，并设置安全上限。
- 文件系统满、删除记录、Factory Reset 等破坏性场景默认不得操作用户真实数据。优先使用隔离目录、合成管理数据或可撤销的故障注入；不能安全隔离时，先说明风险并由用户决定。
- UTEST 的超时通常通过终止测试线程实现。持锁、写 Flash 或占用业务资源的用例必须提供可靠的 `exit` 清理，不能依赖强制终止恢复一致性。
- 设备 UTEST 提供目标环境中的通过/失败证据，不等同于 ASan/UBSan 或源码覆盖率。除非目标构建确实开启并导出覆盖率数据，不得声称获得真机代码覆盖率。

## 编译与执行

- 开启告警并将新增告警视为失败。
- 调试验证使用 `-g -O0` 或 `-Og`、`-fno-omit-frame-pointer`。
- 内存与未定义行为检查使用 ASan/UBSan；覆盖率使用匹配编译器版本的 gcov。
- Sanitizer 与 coverage 可分两轮执行，避免工具组合影响结果判断。
- 任一断言、编译、Sanitizer 或覆盖率生成失败时返回非零状态。

## 覆盖率口径

只统计目标业务源码，不统计 harness、mock、断言和测试入口。

报告中必须分开列出：

- 原始业务 API 的行覆盖率和分支覆盖率。
- 抽取辅助函数的行覆盖率和分支覆盖率。
- 仅完成静态接线审计的接口。
- 尚需真机或集成测试覆盖的接口。

若某 API 因当前宏配置存在不可达旧分支，应说明分母来源和不可达原因，不用无意义用例制造虚假 100%。“执行成功”不能表述为“业务正确”，必须结合断言范围说明结论。

## 项目内测试落点

优先放在模块自己的 `test/` 目录，并让单条命令完成依赖检查、构建、运行、覆盖率生成和清理。例如本工作区的活动识别心率测试位于：

`packages/services/sport/test/activity_spor_ar/activity_hr_snapshot_test.js`

使用该示例时，应重点复用其真实源码抽取、容量不足注入、48 小时边界、ASan/UBSan 和逐 API gcov 统计方式；不要固化其项目宏、结构体或路径到其他模块。

## 输出

最终说明应包含：

- 测试对象与源码版本。
- 主机、编译器、Sanitizer、gcov 及脚本运行时版本。
- 执行命令和可复核的日志/产物位置。
- 用例通过情况。
- 原始业务 API 的逐函数覆盖率。
- 真机 UTEST 的固件配置、设备命令、功能/压力参数、结果日志和清理状态。
- 未覆盖原因、主机测试边界、真机 UTEST 边界和端到端验证项。
